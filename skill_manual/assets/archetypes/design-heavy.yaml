# DEPRECATED (as of 2026-04-25)
# design-heavy archetype は削除され、project.design_focus フラグに統合されました。
#
# どのアーキタイプでも design_focus=true にすると、以下が自動適用されます:
#   - review.specialized_reviewers に ui / a11y を自動追加
#   - CLAUDE.md テンプレに design review workflow 節を追加
#   - production-saas なら visual-regression hook を推奨
#
# 古い profile.json (archetype_primary: design-heavy) を migrate するには:
#   archetype_primary を "production-saas" または "library-package" に変更し、
#   project.design_focus: true を設定してください。
#
# この YAML ファイルは apply_scaffold.py に検出されたとき error を返すためだけに存在します。
# 将来的に削除予定。

name: design-heavy
status: deprecated
description: |
  ⚠ Deprecated. archetype ではなく project.design_focus フラグを使ってください。
  apply_scaffold.py はこの archetype を検出すると error で中断します。

extends: null
templates: []
conditional_templates: []
variables: {}
subagents: []
hooks: {}
